class User {
    constructor(id, firstName, email, password) {
            this.id = id;
            this.userName = firstName;
            this.email = email;
            this.password = password;
            //role
    }
}

module.exports = User;