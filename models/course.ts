class Course {
    constructor(id, title, description, defTime, lessonsNum) {
            this.id = id;
            this.title = title;
            this.description = description;
            this.defTime = defTime;
            this.lessonsNum = lessonsNum;
    }
}

module.exports = Course;